# outputs5/regular_ndcg16 — ListMLE on W/L record standings; best NDCG@16 (phase6 combo 19).
# Run: --config config/outputs5_regular_ndcg16.yaml --outputs outputs5/regular_ndcg16

paths:
  outputs: "outputs5/regular_ndcg16"

training:
  listmle_target: "final_rank"  # W/L record standings
  rolling_windows: [15, 30]

model_a:
  epochs: 25

model_b:
  xgb:
    n_estimators: 236
    max_depth: 5
    learning_rate: 0.08
  lr:
    C: 1.0
    max_iter: 1000

inference:
  run_id_base: 26
