# outputs5/regular_rank_rmse — ListMLE on W/L record standings; best rank RMSE (standings).
# Run: --config config/outputs5_regular_rank_rmse.yaml

paths:
  outputs: "outputs5/regular_rank_rmse"

training:
  listmle_target: "final_rank"  # W/L record standings
  rolling_windows: [15, 30]

model_a:
  epochs: 24

model_b:
  xgb:
    n_estimators: 230
    max_depth: 5
    learning_rate: 0.083
  lr:
    C: 1.0
    max_iter: 1000

inference:
  run_id_base: 26
