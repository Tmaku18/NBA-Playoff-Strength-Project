# outputs5/playoff_rank_rmse — ListMLE on Playoff Outcome Rank; best rank RMSE (20260212 combo 18).
# Run: --config config/outputs5_playoff_rank_rmse.yaml

paths:
  outputs: "outputs5/playoff_rank_rmse"

training:
  listmle_target: "playoff_outcome"  # Playoff Outcome Rank
  rolling_windows: [10, 30]

model_a:
  epochs: 28

model_b:
  xgb:
    n_estimators: 276
    max_depth: 3
    learning_rate: 0.08
  lr:
    C: 1.0
    max_iter: 1000

inference:
  run_id_base: 26
